<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Booking Tours</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#"><%= t(".home")%></a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="#"><%= t(".booking_tours")%></a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="#"><%= t(".reviews")%></a>
        </li>
      </ul>
      <% if user_signed_in? %>
        <%= link_to "Đăng xuất", destroy_user_session_path, method: :delete, class: "navbar-text"%>
      <% else %>
        <span class="navbar-text">
          <%= link_to t(".login"), new_user_session_path %>
        </span> &nbsp;
        <span class="navbar-text">
          <%= link_to t(".registration"), new_user_registration_path %>
        </span>
      <% end %>
    </div>
  </div>
</nav>
